run_name: "actual_run"

# Input directory for two-sample paired results
significance_dir_two_sample_paired: "/scratch/gpfs/AMOELLER/diet_manip/AlleleFlux_final_copy_dnds/longitudinal/significance_tests/two_sample_paired_pre_end-fat_control"
# Input directory for single-sample results
significance_dir_single_sample: "/scratch/gpfs/AMOELLER/diet_manip/AlleleFlux_final_copy_dnds/longitudinal/significance_tests/single_sample_pre_end-fat_control"

# Inputs for quality_control.py
# Root directory with *_metadata.tsv files
metadata_dir: "/scratch/gpfs/AMOELLER/diet_manip/AlleleFlux_final_copy_dnds/longitudinal/inputMetadata/inputMetadata_pre_end-fat_control"
qc_dir: "/scratch/gpfs/AMOELLER/diet_manip/AlleleFlux_final_copy_dnds/longitudinal/QC/QC_pre_end-fat_control"
# Fasta file for MAGs
fasta: "/scratch/gpfs/AMOELLER/diet_manip/redo_representative_megamag.fa"
# MAG to contig mapping file
mag_mapping: "/scratch/gpfs/AMOELLER/diet_manip/megamag_mapping.tsv"

# p_value summary directory (contains all p_value_summary_*.tsv files)
p_value_summary_dir: "/scratch/gpfs/AMOELLER/diet_manip/AlleleFlux_final_copy_dnds/longitudinal/p_value_summary/pre_end-fat_control_rerun"

# Output
output_dir: "/scratch/gpfs/AMOELLER/sidd/diet_manip/mega_table"

# Parameters
breadth_threshold: 0.1
cpus: 16

# Groups for single sample analysis. The extract_mag_positions.py script
# will generate a file for each group.
# Example: ["fat", "control"]
single_sample_groups: ["fat", "control"]
# single_sample_groups: ["fat"]


